configuration:
  log_level:
    name: Log level
    description: >-
      Add-on logging verbosity. Everything else about this hub — whether the
      Home shares anything with Locale Cloud, firmware updates, the Tempest
      weather listener, device time — is decided in the Locale app on the
      owner's phone, not here.
